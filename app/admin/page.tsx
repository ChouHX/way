"use client";

import { FormEvent, InputHTMLAttributes, TextareaHTMLAttributes, useEffect, useState } from "react";
import { Plus, Save, ShieldCheck } from "lucide-react";

type Category = { id: string; slug: string; name_zh: string; name_en: string; sort_order: number };
type CaseStudy = { id: string; title_zh: string; title_en: string; category_name_zh?: string; image_url?: string; published: number };

async function api(resource: string, init?: RequestInit) { const response = await fetch(`/api/admin?resource=${resource}`, { ...init, headers: { "Content-Type": "application/json", ...(init?.headers ?? {}) } }); if (!response.ok) throw new Error(await response.text()); return response.json(); }

export default function AdminPage() {
  const [site, setSite] = useState({ titleZh: "", titleEn: "" }); const [categories, setCategories] = useState<Category[]>([]); const [cases, setCases] = useState<CaseStudy[]>([]); const [status, setStatus] = useState("正在验证管理员权限…");
  const load = async () => { try { const [siteData, categoryData, caseData] = await Promise.all([api("site"), api("categories"), api("cases")]); setSite({ titleZh: siteData.settings.title_zh ?? "", titleEn: siteData.settings.title_en ?? "" }); setCategories(categoryData.categories); setCases(caseData.cases); setStatus("已连接 D1 管理数据"); } catch { setStatus("无法访问后台。请确认 Cloudflare Access 已登录，且邮箱已列入 ADMIN_EMAILS。"); } };
  useEffect(() => { void load(); }, []);
  const saveSite = async (event: FormEvent) => { event.preventDefault(); await api("site", { method: "POST", body: JSON.stringify({ action: "site", ...site }) }); setStatus("站点标题已保存"); };
  const addCategory = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); await api("categories", { method: "POST", body: JSON.stringify({ action: "category", slug: form.get("slug"), nameZh: form.get("nameZh"), nameEn: form.get("nameEn"), sortOrder: Number(form.get("sortOrder") || 0) }) }); event.currentTarget.reset(); await load(); };
  const addCase = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); await api("cases", { method: "POST", body: JSON.stringify({ action: "case", categoryId: form.get("categoryId"), titleZh: form.get("titleZh"), titleEn: form.get("titleEn"), summaryZh: form.get("summaryZh"), summaryEn: form.get("summaryEn"), contentZh: form.get("contentZh"), contentEn: form.get("contentEn"), imageUrl: form.get("imageUrl"), published: true }) }); event.currentTarget.reset(); await load(); };
  return <main className="min-h-screen bg-slate-50 px-5 py-10 text-slate-900"><div className="mx-auto max-w-6xl"><header className="flex flex-col justify-between gap-4 border-b border-slate-200 pb-7 sm:flex-row sm:items-end"><div><p className="text-xs font-bold tracking-[.14em] text-blue-600">YONGSHENG ADMIN</p><h1 className="mt-2 text-3xl font-bold tracking-[-.03em]">站点内容管理</h1></div><p className="flex items-center gap-2 text-sm text-slate-500"><ShieldCheck size={16} className="text-blue-600"/>{status}</p></header><div className="mt-8 grid gap-6 lg:grid-cols-2"><section className="border border-slate-200 bg-white p-6"><h2 className="text-lg font-bold">站点标题</h2><form className="mt-5 grid gap-4" onSubmit={saveSite}><Input label="中文标题" value={site.titleZh} onChange={value => setSite(current => ({ ...current, titleZh: value }))}/><Input label="English title" value={site.titleEn} onChange={value => setSite(current => ({ ...current, titleEn: value }))}/><button className="inline-flex w-fit items-center gap-2 bg-blue-600 px-4 py-2.5 text-sm font-bold text-white"><Save size={16}/>保存标题</button></form></section><section className="border border-slate-200 bg-white p-6"><h2 className="text-lg font-bold">分类管理</h2><form className="mt-5 grid gap-3" onSubmit={addCategory}><Input name="nameZh" label="中文名称" required/><Input name="nameEn" label="English name" required/><Input name="slug" label="Slug" required/><Input name="sortOrder" label="排序" type="number"/><button className="inline-flex w-fit items-center gap-2 border border-slate-300 px-4 py-2.5 text-sm font-bold"><Plus size={16}/>添加分类</button></form><div className="mt-5 divide-y border-y border-slate-200">{categories.map(category => <p key={category.id} className="py-3 text-sm"><b>{category.name_zh}</b><span className="ml-2 text-slate-500">{category.name_en} · {category.slug}</span></p>)}</div></section></div><section className="mt-6 border border-slate-200 bg-white p-6"><h2 className="text-lg font-bold">新增案例</h2><form className="mt-5 grid gap-4 md:grid-cols-2" onSubmit={addCase}><Input name="titleZh" label="案例中文标题" required/><Input name="titleEn" label="Case title (English)" required/><label className="grid gap-2 text-sm font-bold">分类<select name="categoryId" className="border border-slate-300 bg-white px-3 py-3 text-sm"><option value="">未分类</option>{categories.map(category => <option key={category.id} value={category.id}>{category.name_zh} / {category.name_en}</option>)}</select></label><Input name="imageUrl" label="图片 URL"/><TextArea name="summaryZh" label="中文摘要" required/><TextArea name="summaryEn" label="English summary" required/><TextArea name="contentZh" label="中文正文"/><TextArea name="contentEn" label="English content"/><button className="inline-flex w-fit items-center gap-2 bg-[#0f2747] px-4 py-2.5 text-sm font-bold text-white"><Plus size={16}/>发布案例</button></form></section><section className="mt-6 border border-slate-200 bg-white p-6"><h2 className="text-lg font-bold">已保存案例</h2><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{cases.map(item => <article key={item.id} className="border border-slate-200 p-4"><p className="text-xs font-bold text-blue-600">{item.category_name_zh ?? "未分类"}</p><h3 className="mt-2 font-bold">{item.title_zh}</h3><p className="mt-1 text-sm text-slate-500">{item.title_en}</p></article>)}</div></section></div></main>;
}

function Input({ label, value, onChange, ...props }: { label: string; value?: string; onChange?: (value: string) => void } & Omit<InputHTMLAttributes<HTMLInputElement>, "value" | "onChange">) { return <label className="grid gap-2 text-sm font-bold"><span>{label}</span><input {...props} value={value} onChange={event => onChange?.(event.target.value)} className="border border-slate-300 px-3 py-3 text-sm font-normal outline-none focus:border-blue-600 focus:ring-2 focus:ring-blue-100"/></label>; }
function TextArea(props: TextareaHTMLAttributes<HTMLTextAreaElement> & { label: string }) { const { label, ...input } = props; return <label className="grid gap-2 text-sm font-bold"><span>{label}</span><textarea {...input} rows={4} className="resize-y border border-slate-300 px-3 py-3 text-sm font-normal outline-none focus:border-blue-600 focus:ring-2 focus:ring-blue-100"/></label>; }
